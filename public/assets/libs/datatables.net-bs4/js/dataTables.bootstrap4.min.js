// DataTables Bootstrap 4 JS Placeholder
console.log('DataTables Bootstrap 4 placeholder loaded');

// Load from CDN
(function() {
    if (typeof window !== 'undefined' && typeof jQuery !== 'undefined') {
        const script = document.createElement('script');
        script.src = 'https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap4.min.js';
        script.onload = function() {
            console.log('DataTables Bootstrap 4 loaded from CDN');
        };
        document.head.appendChild(script);
    }
})();
