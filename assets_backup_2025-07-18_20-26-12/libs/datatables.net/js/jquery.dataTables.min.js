// DataTables Core JS Placeholder
console.log('DataTables Core placeholder loaded');

// Load from CDN
(function() {
    if (typeof window !== 'undefined' && typeof jQuery !== 'undefined') {
        // Load DataTables from CDN
        const script = document.createElement('script');
        script.src = 'https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js';
        script.onload = function() {
            console.log('DataTables Core loaded from CDN');
        };
        document.head.appendChild(script);
    } else {
        console.warn('jQuery not found - DataTables requires jQuery');
    }
})();
